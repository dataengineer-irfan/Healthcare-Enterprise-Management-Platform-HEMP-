server:
  port: 8090

spring:
  application:
    name: hemp-demo-backend
  main:
    allow-bean-definition-overriding: true
  datasource:
    url: ${SPRING_DATASOURCE_URL:jdbc:postgresql://dpg-d9a6of6cjfls739bdv6g-a.oregon-postgres.render.com:5432/ets_sandbox?sslmode=require}
    username: ${SPRING_DATASOURCE_USERNAME:ets_admin}
    password: ${SPRING_DATASOURCE_PASSWORD:HPVHBKPoe640PYItRQPYQueJdcQEEoJz}
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
  flyway:
    enabled: true
    baseline-on-migrate: true
    locations: classpath:db/migration

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
  endpoint:
    health:
      show-details: always

hemp:
  jwt:
    secret: 404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970
    expiration-ms: 86400000 # 24 hours
